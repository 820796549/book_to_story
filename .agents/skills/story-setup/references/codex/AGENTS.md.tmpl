# {项目名} — 拆有用书写文章生产系统（Codex）

## 项目目标

拆有用书，写高质量文章，并让这个过程稳定复用。

竞品只用来学习写法。oh-story 只作为工程底座。

## Skill 路由表

Codex CLI 中优先通过 `$skill-name` 或 `/skills` 调用；自然语言也可触发对应 skill。

| 命令/意图 | Skill | 说明 |
|---|---|---|
| `$story`、拆书 | story | 主入口，自动判断当前任务并路由 |
| `$story-setup`、初始化 | story-setup | 初始化拆书写作项目 |
| `$story-long-analyze`、拆原文、模型提炼、书籍定位 | story-long-analyze | 书籍定位、样文反推、原文拆解、模型提炼 |
| `$story-long-write`、写栏目文章 | story-long-write | 栏目文章成稿（底层解码篇/认知破局篇等） |
| `$story-review`、审查 | story-review | 质量审查、对标验收 |
| `$story-deslop`、去 AI 味 | story-deslop | 表达打磨、去 AI 味 |
| `$story-cover`、封面 | story-cover | 封面生成 |
| `$story-import`、导入 | story-import | 书籍导入、反向解析 |
| `$browser-cdp` | browser-cdp | 浏览器操控、数据采集 |

## 书籍项目结构

仓库根目录是系统模板。每一本书单独放在 `projects/<书名>/`：

```text
projects/<书名>/
  00-项目定义/
  01-样文反推/
  02-原文拆解/
  03-模型提炼/
  04-栏目协议/
  05-文章生产/
  06-质量审查/
  07-方法库/
  08-项目打包/
  参考资料/
  追踪/
```

项目根目录 `.active-book` 写入当前书籍项目路径，例如：

```text
projects/毛选第一卷
```

## 写正文前置条件

写栏目文章前，当前书籍项目内必须存在：

- `00-项目定义/书籍定位.md`
- `00-项目定义/栏目体系.md`
- `04-栏目协议/对应栏目协议.md`
- `01-样文反推/对应样文反推.md`
- `02-原文拆解/对应篇章.md`
- `02-原文拆解/第NN篇_标题_研究素材.md`（如原文拆解中标记了需研究的案例）
- `03-模型提炼/对应模型.md`

## Codex 项目约定

- Codex custom agents 部署在 `.codex/agents/*.toml`，需要新开 Codex 会话后才会稳定可用。
- Codex hooks 部署在 `.codex/hooks.json` 和 `.codex/hooks/`；项目 `.codex/` 层需要被信任，非 managed hooks 还需要在 `/hooks` 中 review/trust 后才会运行。
- 写作顺序不可颠倒：同一篇原文必须先写底层解码篇，再写认知破局篇。
- Compact 后优先读取当前书籍项目的 `追踪/上下文.md` 恢复进度。

## 协作规则

- 一次只做一个明确文件。
- 不跳过定位、反推、原文拆解直接写正文。
- 审查和修是 pipeline 的强制环节。
- 不照搬竞品表达。
- 内容质量以审查报告为准。
